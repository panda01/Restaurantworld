#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../jitsu/bin/jitsu" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../jitsu/bin/jitsu" "$@"
  ret=$?
fi
exit $ret
